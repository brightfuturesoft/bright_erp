interface DataType {
    key: string;
    orderNumber: string;
    saleDate: string;
    customer: string;
    subTotal: string;
    totalTax: string;
    totalDiscount: string;
    grandTotal: string;
    status: boolean;
}

export type { DataType };
